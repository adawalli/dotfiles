# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: capstan
description: Meeting transcript automation daemon
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Cobra([capstan])"]
