# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: nvsec
description: CLI Tool for prodsec
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Click([nvsec])"]
