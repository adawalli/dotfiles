# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: sailpoint-cli
description: SailPoint-Teleport governance CLI
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Cobra([sailpoint-cli])"]
